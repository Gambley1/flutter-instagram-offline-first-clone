name: stories_repository

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - "flutter_instagram_offline_first_clone/packages/stories_repository/**"
      - ".github/workflows/stories_repository.yaml"
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/dart_package.yml@v1
    with:
      dart_sdk: 3.2.6
      working_directory: flutter_instagram_offline_first_clone/packages/stories_repository